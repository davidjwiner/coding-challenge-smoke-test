# Smoke Test

This tiny Next.js app is only for confirming that a candidate can install dependencies and run a local dev server before the live interview.

```bash
npm install
npm run dev
```

Open http://localhost:3000.
